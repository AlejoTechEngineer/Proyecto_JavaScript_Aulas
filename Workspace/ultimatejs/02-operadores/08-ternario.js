//Se compone asi:
//Lo primero es tener claridad de la expresion como por ejemplo si un numero es mayor que otro
//Luego determinar si esa expresion devuelve true o false
//Entonces si devuelve true asignamos el valor de true y por el contrario si es false asignamos el valor de false

let edad = 19;
let acceso = edad > 17 ? "Permitir ingreso" : "Negar ingreso";
console.log(acceso);
//Entonces como se puede ver el operador ternario cumple con la función muy similar a la del operador if que lo vemos en la siguiente sección.
//Donde si la condición envaluada devuelve true coloca el valor que le indiquemos (permitir ingreso en este caso) al igual que si devuelve false devuelve este valor.

