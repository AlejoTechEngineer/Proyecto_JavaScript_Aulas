function tablaMultiplicar(num) {
    let resultado = [];

    for (let i = 1; i <= 10; i++) {
        resultado.push(`${num} x ${i} = ${num * i}`);
    }

    return resultado;
}
console.log(tablaMultiplicar(5));
